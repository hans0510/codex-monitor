# Quick Task 260708-f57: Publish separate GitHub releases for Linux CLI and Windows Lulu desktop builds

## Scope

Publish two GitHub Releases for the current repository state:

- `linux-cli-v0.1.0`: Linux x86_64 CLI build.
- `windows-lulu-v0.1.0`: Windows x86_64 desktop companion build with the Lulu/capybara UI.

## Tasks

1. Build and verify release binaries.
   - Verify: `cargo test`, Linux musl release build, and Windows desktop release build complete.
2. Package release assets under `dist/`.
   - Verify: archives and SHA-256 checksum files exist.
3. Create tags and GitHub releases with uploaded assets.
   - Verify: GitHub release API returns release URLs and remote tags exist.
4. Record release results in GSD summary/state.
   - Verify: quick summary references asset names, tags, and release URLs.
